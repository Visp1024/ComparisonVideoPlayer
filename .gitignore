bin/
obj/
# Нативные библиотеки FFmpeg (n9.0 win64 gpl shared), качаются отдельно — см. tools/publish.ps1
tools/ffmpeg/
publish/
*.user
.vs/
# Мокапы визуального компаньона брейншторма
.superpowers/
# Рабочие документы и артефакты фаз в репозитории не хранятся — от docs остались
# только картинки, на которые ссылается README.
docs/*
!docs/screenshot.png
!docs/app-icon.svg
# Метка рабочей копии пула, её кладёт доска Makestead — в репозитории ей делать нечего.
.kanban-slot
